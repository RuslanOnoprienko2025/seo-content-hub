---
const { title, description, image } = Astro.props;
---
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
    <meta name="description" content={description} />
    {image && <meta property="og:image" content={image} />}
    <style is:global>
        /* Основные стили для читабельности лонгрида */
        body { 
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            line-height: 1.7; 
            color: #24292e; 
            max-width: 740px; /* Оптимальная ширина для чтения текста */
            margin: 0 auto; 
            padding: 40px 20px; 
            background: #fff;
        }
        h1 { font-size: 2.2rem; line-height: 1.3; margin-bottom: 1.5rem; color: #111; }
        h2 { font-size: 1.6rem; margin-top: 2.5rem; margin-bottom: 1rem; color: #111; }
        h3 { font-size: 1.3rem; margin-top: 2rem; color: #222; }
        
        /* Стили для твоих списков (из раздела "What Should Be in Email Signature?") */
        ul, ol { margin-bottom: 1.5rem; padding-left: 1.5rem; }
        li { margin-bottom: 0.5rem; }

        /* Стили для ссылок (чтобы твои CTA-кнопки выделялись) */
        a { color: #0070f3; text-decoration: underline; text-underline-offset: 4px; }
        
        /* Стили для цитат или выделенных кейсов (Case Studies) */
        blockquote { 
            border-left: 4px solid #0070f3; 
            margin: 2rem 0; 
            padding: 10px 20px; 
            background: #f0f7ff;
            font-style: italic;
        }

        img { max-width: 100%; height: auto; border-radius: 8px; display: block; margin: 2rem 0; }
        
        /* Тонкая линия разделения разделов */
        hr { border: 0; border-top: 1px solid #eee; margin: 3rem 0; }
    </style>
</head>
<body>
    <nav style="margin-bottom: 50px;"><a href="/">← На главную</a></nav>
    <slot />
</body>
</html>
